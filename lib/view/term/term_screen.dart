import 'package:flutter/cupertino.dart';

class TermScreen extends StatelessWidget {
  final String term;
  const TermScreen({required this.term, super.key});

  @override
  Widget build(BuildContext context) {
    return Text('term screen');
  }
}
