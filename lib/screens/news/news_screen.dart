import 'package:flutter/cupertino.dart';

class NewsScreen extends StatelessWidget {
  const NewsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('newsscreen');
  }
}
